name: pokedex
version: "1.0.0"
config-version: 2

profile: pokedex

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  pokedex:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
